{
  "name": "moge-3d-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "mkdir -p public/litert-wasm && cp node_modules/@litertjs/core/wasm/* public/litert-wasm/",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@litertjs/core": "^2.5.3",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
